from typing import List, Tuple


def build_index(chunks: List[str]) -> None:
    # Stub: build FAISS/SQLite-vec index later
    return None


def search(query: str, k: int = 5) -> List[Tuple[int, float]]:
    # Stub: return (chunk_id, score)
    return []
