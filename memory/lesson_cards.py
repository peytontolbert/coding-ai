from typing import Dict, Any, List


def record_lesson(card: Dict[str, Any]) -> None:
    # Stub: write to JSONL later
    return None


def retrieve_lessons(query: Dict[str, Any], k: int = 5) -> List[Dict[str, Any]]:
    # Stub: return empty for now
    return []
